package dialog_test

import "testing"

func TestLineage(t *testing.T) {
	t.Parallel()

	t.Run("complete lineage", func(t *testing.T) {
		t.Parallel()
	})

	t.Run("some branches missing", func(t *testing.T) {
		t.Parallel()
	})

	t.Run("empty lineage", func(t *testing.T) {
		t.Parallel()
	})
}
