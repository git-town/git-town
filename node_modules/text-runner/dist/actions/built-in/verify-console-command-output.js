"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const assertNoDiff = __importStar(require("assert-no-diff"));
const running_console_command_1 = require("../helpers/running-console-command");
function verifyConsoleCommandOutput(args) {
    args.name("verifying the output of the last run console command");
    const expectedLines = args.nodes
        .text()
        .split("\n")
        .map(line => line.trim())
        .filter(line => line);
    const actualLines = running_console_command_1.RunningConsoleCommand.instance()
        .output.fullText()
        .split("\n")
        .map(line => line.trim())
        .filter(line => line);
    const commonLines = actualLines.filter(line => expectedLines.includes(line));
    assertNoDiff.trimmedLines(expectedLines.join("\n"), commonLines.join("\n"));
}
exports.default = verifyConsoleCommandOutput;
//# sourceMappingURL=verify-console-command-output.js.map