"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const colorette_1 = __importDefault(require("colorette"));
const path_1 = __importDefault(require("path"));
/** The "cd" action changes the current working directory to the one given in the hyperlink or code block. */
function cd(args) {
    const directory = args.nodes.text();
    args.name(`changing into the ${colorette_1.default.bold(colorette_1.default.cyan(directory))} directory`);
    const fullPath = path_1.default.join(args.configuration.workspace, directory);
    args.log(`cd ${fullPath}`);
    try {
        process.chdir(fullPath);
    }
    catch (e) {
        if (e.code === "ENOENT") {
            throw new Error(`directory ${directory} not found`);
        }
        throw e;
    }
}
exports.default = cd;
//# sourceMappingURL=cd.js.map