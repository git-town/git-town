"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_extra_1 = __importDefault(require("fs-extra"));
const html_parser_1 = require("./html-parser");
/** returns the standard AST for the HTML files with the given paths */
async function parseHTMLFiles(filenames, tagMapper) {
    const result = [];
    const parser = new html_parser_1.HTMLParser(tagMapper);
    for (const filename of filenames) {
        const content = await fs_extra_1.default.readFile(filename.platformified(), {
            encoding: "utf8"
        });
        result.push(parser.parse(content, filename, 1));
    }
    return Promise.all(result);
}
exports.parseHTMLFiles = parseHTMLFiles;
//# sourceMappingURL=parse-html-files.js.map