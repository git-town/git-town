"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const chai_1 = require("chai");
const delay_1 = __importDefault(require("delay"));
const stats_counter_1 = require("./stats-counter");
suite("StatsCounter", function () {
    test("counting errors", function () {
        const counter = new stats_counter_1.StatsCounter();
        counter.error();
        counter.error();
        chai_1.assert.equal(counter.errors(), 2);
    });
    test("counting skips", function () {
        const counter = new stats_counter_1.StatsCounter();
        counter.skip();
        counter.skip();
        chai_1.assert.equal(counter.skips(), 2);
    });
    test("counting successes", function () {
        const counter = new stats_counter_1.StatsCounter();
        counter.success();
        counter.success();
        chai_1.assert.equal(counter.successes(), 2);
    });
    test("counting the time", async function () {
        const counter = new stats_counter_1.StatsCounter();
        await delay_1.default(1);
        chai_1.assert.match(counter.duration(), /\d+.s/);
    });
});
//# sourceMappingURL=stats-counter.test.js.map