import { Activity } from "../activity-list/types/activity";
import { Configuration } from "../configuration/types/configuration";
import { Formatter } from "./types/formatter";
/** A formatter that prints output and step names */
export declare class DetailedFormatter implements Formatter {
    /** the directory in which the sources are located */
    private readonly configuration;
    constructor(stepCount: number, configuration: Configuration);
    failed(activity: Activity, stepName: string, e: Error, output: string): void;
    skipped(activity: Activity, stepName: string, output: string): void;
    success(activity: Activity, stepName: string, output: string): void;
}
//# sourceMappingURL=detailed-formatter.d.ts.map