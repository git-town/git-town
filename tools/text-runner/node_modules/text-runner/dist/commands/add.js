"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_extra_1 = __importDefault(require("fs-extra"));
const path_1 = __importDefault(require("path"));
async function addCommand(blockName) {
    if (!blockName) {
        throw new Error("no block name given");
    }
    let textRunDirExists = true;
    try {
        await fs_extra_1.default.stat("text-run");
    }
    catch (e) {
        textRunDirExists = false;
    }
    if (!textRunDirExists) {
        await fs_extra_1.default.mkdir("text-run");
    }
    await fs_extra_1.default.writeFile(path_1.default.join("text-run", blockName + ".js"), template(blockName), "utf8");
    return [];
}
exports.addCommand = addCommand;
function template(filename) {
    return `module.exports = async function (args) {
  console.log('This code runs inside the "${filename}" block implementation.')
  console.log('I found these elements in your document:')
  console.log(args.nodes)

  // capture content from the document
  // const content = activity.searcher.tagContent('boldtext')
  // do something with the content
  // args.log(content)
}
`;
}
//# sourceMappingURL=add.js.map