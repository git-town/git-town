"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const assertNoDiff = __importStar(require("assert-no-diff"));
const colorette_1 = __importDefault(require("colorette"));
const fs_extra_1 = __importDefault(require("fs-extra"));
const path_1 = __importDefault(require("path"));
async function verifyWorkspaceFileContent(args) {
    const filePath = args.nodes.textInNodeOfType("strong", "em");
    const fullPath = path_1.default.join(process.cwd(), filePath);
    args.name(`verifying file ${colorette_1.default.cyan(filePath)}`);
    args.log(`verify file ${fullPath}`);
    const actualContent = await readFile(filePath, fullPath);
    const expectedContent = args.nodes.textInNodeOfType("fence", "code");
    try {
        assertNoDiff.trimmedLines(actualContent.trim(), expectedContent.trim());
    }
    catch (err) {
        throw new Error(`mismatching content in ${colorette_1.default.cyan(colorette_1.default.bold(filePath))}:\n${err.message}`);
    }
}
exports.default = verifyWorkspaceFileContent;
async function readFile(filePath, fullPath) {
    try {
        const result = await fs_extra_1.default.readFile(fullPath, "utf8");
        return result;
    }
    catch (err) {
        if (err.code === "ENOENT") {
            throw new Error(`file ${colorette_1.default.red(filePath)} not found`);
        }
        else {
            throw err;
        }
    }
}
//# sourceMappingURL=verify-workspace-file-content.js.map