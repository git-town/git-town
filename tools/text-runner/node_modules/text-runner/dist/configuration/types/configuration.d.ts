import { Publications } from "../publications/publications";
/**
 * All configuration values that are provided to the application
 * after analyzing the configuration provided by the user
 * and determining other runtime variables.
 */
export interface Configuration {
    actions: any;
    classPrefix: string;
    defaultFile: string;
    exclude: string | string[];
    fileGlob: string;
    formatterName: string;
    keepTmp: boolean;
    publications: Publications;
    offline: boolean;
    sourceDir: string;
    useSystemTempDirectory: boolean;
    workspace: string;
}
/** creates an empty configuration object for testing */
export declare function scaffoldConfiguration(): Configuration;
//# sourceMappingURL=configuration.d.ts.map