"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const debug_1 = __importDefault(require("debug"));
const ps_tree_1 = __importDefault(require("ps-tree"));
const util_1 = __importDefault(require("util"));
const debug = debug_1.default("end-child-processes");
const psTreeA = util_1.default.promisify(ps_tree_1.default);
async function endChildProcesses() {
    const children = await psTreeA(process.pid);
    for (const child of children) {
        if (child.COMMAND === "ps") {
            continue;
        }
        const processID = parseInt(child.PID, 10);
        debug(`ending child process ${processID}: ${child.COMMAND}`);
        try {
            process.kill(processID);
        }
        catch (e) {
            debug(`cannot kill process ${processID} (${child.COMMAND}): ${e.message}`);
        }
    }
}
exports.endChildProcesses = endChildProcesses;
