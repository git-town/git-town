// Package gitconfig provides functionality around storing Git Town configuration information as Git metadata.
// Git Town stores configuration in two locations:
// 1. Git metadata of the local repository. You can see it with `git config -l --local`
// 2. Global Git metadata. You can seet it with `git config -l --global`
package gitconfig
