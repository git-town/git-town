machine:
  ruby:
    version: 2.2.0
  environment:
    PATH: ~/.cabal/bin/:$PATH


dependencies:
  cache_directories:
    - "~/.cabal" # Keep shellcheck around between builds
  post:
    - bin/circleci-install-shellcheck


test:
  override:
    - bundle exec rake
