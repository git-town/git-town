#!/bin/bash -e

# Install ShellCheck
sudo apt-get install -q cabal-install
cabal update --help
cabal install --help
cabal update
cabal install shellcheck
export PATH=~/.cabal/bin/:$PATH

# Run the full test suite
export PATH=$(pwd):$PATH
bundle exec rake
